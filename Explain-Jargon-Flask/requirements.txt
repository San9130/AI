# Core web framework
flask==3.0.3

# OpenAI SDK for GPT-5 + TTS 
openai==1.99.5

# Convert plain text to Unicode Braille
pybraille==1.0.0

# Load environment variables from .env file
python-dotenv==1.0.1

# WSGI HTTP Server (for deployment)
gunicorn==22.0.0
