# Core web framework
flask==3.0.3

# OpenAI SDK for GPT + TTS
openai==1.30.1

# Convert plain text to Unicode Braille
pybraille==0.1.2

# Load environment variables from .env file
python-dotenv==1.0.1

# WSGI HTTP Server (for deployment)
gunicorn==22.0.0
